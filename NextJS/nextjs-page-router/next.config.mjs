/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  reactStrictMode: true,
  i18n: {
    locales: ["en", "np"],
    defaultLocale: "en",
  },
};

export default nextConfig;
