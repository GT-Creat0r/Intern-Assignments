/** @type {import('next').NextConfig} */
const nextConfig = {
  i18n: {
    locales: ["en", "np"], // supported locales
    defaultLocale: "en", // default language
  },
};

export default nextConfig;
